server:
  port: 8082

spring:
  application:
    name: gio-admin

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://8.137.63.159:3306/gio_design?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: "@Yuku007@"

# MyBatis Plus 配置
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.gio.entity
  global-config:
    db-config:
      id-type: auto
  configuration:
    map-underscore-to-camel-case: true

# JWT 配置
jwt:
  secret: gio-design-secret-key-2026-change-this-in-production
  expiration: 86400000  # 24 小时
  header: Authorization
  prefix: Bearer

# 文件上传配置
file:
  upload-path: ./uploads/
  image-types: jpg,jpeg,png,gif,webp

# 日志配置
logging:
  level:
    com.gio: debug
    com.gio.mapper: debug
