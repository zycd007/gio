server:
  port: 8081

spring:
  application:
    name: gio-portal

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://8.137.63.159:3306/gio_design?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: "@Yuku007@"

  # 静态文件配置
  web:
    resources:
      static-locations: classpath:/META-INF/resources/,classpath:/resources/,classpath:/static/,classpath:/public/,file:./uploads/,file:E:\\my_projects\\gio\\uploads\\

# MyBatis Plus 配置
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.gio.entity
  global-config:
    db-config:
      id-type: auto
  configuration:
    map-underscore-to-camel-case: true

# JWT 配置（C 端不需要，但需要占位符避免报错）
jwt:
  secret: placeholder
  expiration: 86400000

# 文件上传配置（占位符）
file:
  upload-path: ./uploads/
  image-types: jpg,jpeg,png,gif,webp

# 邮件配置
spring.mail:
  host: smtp.qq.com
  port: 587
  username: 597667676@qq.com
  password: fymhnpxcresobcah
  properties:
    mail.smtp.auth: true
    mail.smtp.starttls.enable: true
    mail.smtp.starttls.required: true

# 管理员通知邮箱
notification:
  admin-email: 597667676@qq.com

# 日志配置
logging:
  level:
    com.gio: debug
    com.gio.mapper: debug
